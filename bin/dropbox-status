#!/bin/bash
source "$HOME/.config/dropbox-scripte.conf"
for dropbox in $dropboxes
do
    HOME="/home/$USER"
    HOME="$HOME/$dropbox"
    export HOME
    echo -n "$dropbox: "
    $HOME/.dropbox-dist/dropbox status
done
